import 'package:flutter_test/flutter_test.dart';

import 'package:chargewise_my/main.dart';

void main() {
  test('the application root widget is available', () {
    expect(const ChargeWiseApp(), isNotNull);
  });
}
